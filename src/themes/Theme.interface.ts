export interface ThemeProps {
    colors: {
      primary: string,
      secondary: string,
      tertiary: string,
      quaternary: string,
      neutral: string, 
    },
    bodyText: string,
    borderRadius: string,
    borderWidth: string,
    shadowWidth: string
  };
  